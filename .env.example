# Database connection
DB_SERVER=127.0.0.1,1433
DB_NAME=G1StallionMatchProductionV5
DB_USER=your_username
DB_PASSWORD=your_password

# Audit
AUDIT_USER_ID=2

# Golden table rebuild window (not used in scoring, kept for reference)
YEAR_START=2020
YEAR_END=2026

# Model selection (which model directory to use for each country)
AUS_MODEL=aus
NZL_MODEL=nzl
USA_MODEL=usa

# Historical lookback countries (comma-separated)
# Which countries to include for historical sire/dam/vendor metrics
HIST_COUNTRIES_NZL=NZL,AUS

# API Configuration
API_KEY=your-secret-api-key
